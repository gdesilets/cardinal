# Binary research databases belong in Git LFS. Covers both the canonical skill copy
# (.claude/skills/*/data/*.sqlite) and its Codex mirror (.codex/skills/*/data/*.sqlite).
*.sqlite filter=lfs diff=lfs merge=lfs -text
