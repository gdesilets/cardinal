# One-page Jan 8 v2.7 shadow/live pilot protocol

Status: **not enabled**. Scope: exactly Jan 8, 2026 Premium Journals; Discord UI only.

1. Add one explicit disabled route generated by `premium_journals_v2_7_schedule.build_disabled_route("2026-01-08")`. Confirm the route points only to `raw/channel_segments_v2_7` and no Jan 1–7 or Questions path changes.
2. Shadow the full search page-by-page. Before any resolution, persist the normal immutable page plan under `raw/premium_journals_v2_7_checkpoints/2026-01-08/page_NNN` and calculate its byte count and SHA-256. Reject any caller-supplied row/group subset that is not the plan's exact full partition.
3. Before deriving new direct evidence, read any exact direct-checkpoint path and validate its preserved evidence timestamp against the current group and page-plan bytes. Reuse a valid checkpoint byte-for-byte. Treat invalid JSON, tampering, or semantic drift as an immutable conflict; never delete a checkpoint to force a retry. For a new direct candidate, require exact row ownership, exact parent source URL, one non-parent child, complete candidate tuple list, agreement across reply/accessory evidence, and an exclusive direct checkpoint. If any condition fails, use the ordinary v2.6 header click/back/restore proof and validate the entire checkpoint from disk; a flag such as `{exact:true}` is never evidence.
4. Keep all groups provisional until every expected group has exactly one valid resolution checkpoint and the full page membership has not changed. A retry after a later-group failure must reuse earlier direct checkpoints. Never merge a partial page, and never integrate a contiguous page prefix: the accepted page count must equal `ceil(reported_total / 25)`.
5. Independently compare every direct-resolved child with a header-navigation control sample before promotion. Any mismatch, drift, render ambiguity, count drift, checkpoint conflict, or parent/guild mismatch quarantines the pilot and leaves the canonical unpromoted.
6. Run the v2.7 generic QA plus the existing full discovery suite. Supply the validated active v2.6 schedule explicitly, then review direct-vs-header counts, errors, hashes, source-file set, fallback rate, and the no-double-authority result. The Jan 8 run remains a non-promotable shadow; only a separate migration may remove v2.6 authority and enable later v2.7 live routes.
